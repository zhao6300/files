# 推理服务 SLO 与 Goodput（32 页 · 图表版）

面向**领导与非专业听众**的技术普及材料，用于建立概念共识：一次 LLM 推理请求的体验是怎么被量化的、为什么会违约、我们手上有哪些控制手段、以及为什么最终目标是 Goodput。

设计原则：**一页一张主图 + 一句结论**。正文不写长句，细节放在讲稿备注里口头补充。

## 文件说明

| 文件 | 说明 |
|---|---|
| `LLM-Inference-SLO-Goodput-v2.pptx` | 可编辑 PPT，32 页，16:9，浅色风格（英文文件名，便于下载） |
| `推理服务SLO与Goodput_技术分享.pptx` | 同一份 PPT 的中文文件名副本 |
| `preview/s01.png … s32.png` | 逐页预览图，可在 GitHub 上直接翻看 |
| `src/` | 生成脚本，便于批量调整配色与文案 |

## 内容结构

| 页码 | 内容 |
|---|---|
| 1–3 | 封面、只记三句话、内容框架 |
| 4–6 | **Part 1 为什么需要 SLO**：机器视角 vs 用户视角、餐厅类比 |
| 7–12 | **Part 2 核心指标**：两个阶段、TTFT 构成、TPOT 体感、长尾分布、Goodput 曲线 |
| 13–17 | **Part 3 TTFT 由什么决定**：五类因素、队头阻塞、PD 配比曲线、KV 层级对比 |
| 18–22 | **Part 4 TPOT 由什么决定**：Decode 步拆解、干扰散点、Chunk 交叉曲线、PD 场景矩阵 |
| 23–28 | **Part 5 五层控制手段**：总框架、架构层、调度层、延迟预算、执行 / 显存 / 通信 |
| 29–32 | **Part 6 系统级优化**：全景图、工作映射、结语 |

**32 页均附讲稿备注**（PowerPoint 演讲者视图可见）。

## 主要图表

| 页 | 图表 | 说明的问题 |
|---|---|---|
| 9 | 堆叠条形图 | 负载升高时 Prefill 没变慢，涨的是排队 → TTFT ≠ Prefill 时间 |
| 10 | 柱状图 | 50 / 100 / 300 ms 对应「每秒多少字」的体感差别 |
| 11 | 分布图 + SLO 红线 | 平均值达标，长尾仍在违约 |
| 12 | 双曲线 | 吞吐单调上升，Goodput 先升后降，存在最佳工作点 |
| 16 | U 形曲线 | PD 资源配比与 TTFT 非单调，存在最优区间 |
| 17 | 堆叠条形图 | NVMe 命中省下计算，却被搬运延迟推过红线 |
| 20 | 时间轴 + 散点 | Prefill 打断出字；干扰强度与 TPOT 近似线性（R² = 0.99） |
| 21 | 交叉曲线 | Chunk 变大：TTFT 降、TPOT 升 |
| 22 | 场景矩阵 | 三种 SLO 组合下，聚合与分离的结论会反转 |

## 几个贯穿全篇的结论

- **TTFT ≠ Prefill 时间**：排队、调度、KV 读取、传输往往是更大的那一块。
- **平均值会骗人**：要看 p90 / p95 与达标率，并按上下文长度分桶，避免长请求被系统性牺牲。
- **KV 命中不一定更快**：深层（NVMe）恢复的搬运延迟可能反而导致 TTFT 违约。
- **Chunk 大小 / PD 配比不是性能参数**，而是在 TTFT 与 TPOT 之间分配延迟预算。
- **吞吐最高 ≠ Goodput 最高**：最终目标是在 SLO 约束下承载更多请求。

## 重新生成

```bash
pip install python-pptx
cd src && python build.py          # 生成 ../LLM-Inference-SLO-Goodput-v2.pptx
```

可选：逐页自检版式（文字溢出 / 元素越界 / 文字重叠）并刷新预览图。需要 Pillow 与一款中文字体：

```bash
cd src && python render.py         # 输出 ../preview/*.png 并打印警告
```

脚本分工：`deckkit.py` 配色与版式、`charts.py` 图表绘制、`part_a/b/c.py` 页面内容、`notes.py` 讲稿备注。

## 说明

- 正文字号 14～16pt 起步（9.5 / 11pt 仅用于页脚与页码），投屏与打印都便于阅读。
- 字体使用「微软雅黑 + Segoe UI」，Windows 直接正常；macOS 会回退字体，可在 `src/deckkit.py` 中把 `EA` 改为 `PingFang SC` 或 `Source Han Sans SC`。
- 第 9、17 页的构成比例为**示意图**，页面上已标注「非实测数据」，建议替换为我们自己的实测构成。
- 第 19 页涉及 MoE / PCIe 的内容已标注为**工程观测，不属于论文结论**。
- 引用的实验数字（达标率 98% / 97%、干扰强度拟合 R² = 0.99）来自分享大纲，正式对外汇报前建议再核对原始出处的口径。
